# Strict configuration to prevent I/O issues
cache=${HOME}/.npm-cache
registry=https://registry.npmjs.org/

# Reduce network and I/O overhead
progress=false
loglevel=error
maxsockets=4

# Prevent permission issues
unsafe-perm=true
